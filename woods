#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <stdbool.h> 
#include "errorCheck.h"
#include <unistd.h>

int woods(){
  int choice, rc;
  printf("Welcome to the woods!\nGold Coins: %d\n", x);
  printf("You have found a map conviniently placed by a log!!\nThe map shows you different areas of the woods and places to explore... MORE TEXT");
  sleep(2);
  printf("Where would you like to go?\nMia S's Apothecary (1)\nNic's Wizard Tower (2)\nExplore a Different Land (3)\nContinue on your Travels/Leave (0)\n");
  rc = scanf("%d", &choice);
  choice = errorCheck(rc, choice, 3);
  if (choice == 1){
    int apothecary_choice;
    printf("Welcome to Mia S's Apothecary!\n"); 
    sleep(2);
    printf("Would you like to: \nBrew a potion (1)\nBuy a cursed object (2)\nReturn to the woods (0)");
    rc = scanf("%d", &apothecary_choice);
    apothecary_choice = errorCheck(rc, apothecary_choice, 2);
    if (apothecary_choice == 0){
      printf("You chose to return to the woods.");
      return 5;
    }
    else if (apothecary_choice == 1){
      printf("TEXT");
      return 5;
    }
    else if (apothecary_choice == 2){
      printf("TEXT");
      return 5;
    }
  }
  else if (choice == 2){
    int tower_choice;
    printf("Welcome to Nic's Wizard Tower!\n"); 
    sleep(2);
    printf("Would you like to: \nHear a prophecy (1)\nRequest a spell to be cast (2)\nReturn to the woods (0)");
    rc = scanf("%d", &tower_choice);
    tower_choice = errorCheck(rc, tower_choice, 2);
    if (tower_choice == 0){
      printf("You chose to return to the woods.");
      return 5;
    }
    else if (tower_choice == 1){
      printf("TEXT");
      return 5;
    }
    else if (tower_choice == 2){
      printf("TEXT");
      return 5;
    }
  }
  else if (choice == 3){
    int explore_choice;
    printf("You chose to explore a different Land.\nWhere would you like to go?\n");
    sleep(2);
    printf("The Common Land (1)\nTiffany's Kingdom 2 (2)\nNick's Kingdom (3)\nThe Rafferty Kingdom (4)\n");
    rc = scanf("%d", &explore_choice);
    explore_choice = errorCheck(rc, explore_choice, 4);
    if (explore_choice == 1){
      return 1;
    }
    if (explore_choice == 2){
      return 2;
    }
    if (explore_choice == 3){
      return 3;
    }
    if (explore_choice == 4){
      return 4;
    }
  }
  else if(choice == 0){
    return 0;
  }
}
