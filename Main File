#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <stdbool.h> 
#include "errorCheck.h"
#include <unistd.h>

int errorCheck(int a, int b, int j){
  while ((!a) || (b < 0) || (b > j)){
    if (!a){
      scanf("%*s");
    }
    printf("Please try again: ");
    a = scanf("%d", &b);
  }
  return b;
}

int currency = 100;
bool visited_gala = false;

int i;
char printLine[200];

int main(){
  int choice;
  srand(time(NULL));
  printf("Welcome to the Pioneer Plains! Enjoy freely exploring our vast lands.\n");
  choice = home();
  while (choice != 0){
    if (choice == 1){
      choice = home();
    }
    else if (choice == 2){
      choice = kingdom1();
    }
    else if (choice == 3){
      choice = kingdom2();
    }
    else if (choice == 4){
      choice = kingdom3();
    }
    else{
      choice = woods();
    }
  }
  if (choice == 0){
    printf("Thank you for visiting our lands! We hope you enjoyed your time here.\n");
    sleep(1);
    printf("Good luck on the rest of your journey traveller...\n");
    return 0;
  }
  return 0;
}
